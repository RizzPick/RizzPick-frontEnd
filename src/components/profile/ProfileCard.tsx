import { UserInfo } from '@/types/user'
import React from 'react'

type Props = {
    profile : UserInfo
}

function ProfileCard({ profile : { data }}: Props) {
  return (
    <></>
  )
}

export default ProfileCard